import { Box, LinearProgress } from '@mui/material';

const ProgressLoader = () => {
  return (
    <Box sx={{ width: '100%' }}>
      <LinearProgress />
    </Box>

  );
};

export default ProgressLoader;